<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seven.dao.NoticeDao">

    <select id="getAllNotice" resultType="com.seven.domain.Notice">
        select * from notice;
    </select>

<!--    <insert id="addNotice">-->
<!--        insert into notice()-->
<!--    </insert>-->

    <delete id="deleteNotice" parameterType="list">
        delete from notice where id in
        <foreach collection="list" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <update id="updateNotice" parameterType="Object">
        update notice set title=#{param2} where id=#{param1};
        update notice set content=#{param3} where id=#{param1};
    </update>

</mapper>